name: lhb

on:
  push:              # 每次 push 时触发
  schedule:
    - cron: '0 */1 * * *' # 每1小时运行一次
  workflow_dispatch:      # 允许手动触发

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Setup workspace
        run: |
          cd $GITHUB_WORKSPACE
          if [ ! -d .git ]; then
            git init
            git remote add origin https://github.com/gslege/lhb.git
          fi
          git fetch origin main
          git reset --hard origin/main

      - name: Install dependencies and run
        run: |
          cd $GITHUB_WORKSPACE
          python3 -m pip install -q requests
          python3 lhb.py

      - name: Push results
        run: |
          cd $GITHUB_WORKSPACE
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # 只 add 实际生成的文件（lhb.txt 可能不存在）
          git add lhb.html 2>/dev/null || true
          if [ -f lhb.txt ]; then git add lhb.txt; fi
          git diff --cached --quiet || git commit -m "Auto update lhb [skip ci]"
          git push origin main || true
